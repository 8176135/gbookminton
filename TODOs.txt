IP rate limits per action
Email user on removal from waitlist
Email user when they are moved from waitlist to the actual list
Difference costs for event depending on account type. Two account types, (company, plus-one)

Currently there is a special page when you click on an event in admin mode. This page should be made public, but have edit options hidden for non-admins.
An event should also be allowed to be set as private, so public users can't see the list of people already signed up, only the number and number of people in the waitlist.
Also the admin should be allowed to edit all the event details.


Make sure name and email has trailing and prefix whitespaces trimed.


Let's do a refactor, I want to use shadcn presets for a lot of our components to have a consistent style and avoid rewriting common components.

In addition to the general replacements (look at what is reasonable, and give me a list you think is appropriate to refactor before starting),
we should also replace the event datetime and deadline datetime inputs with the calendar component.

Add the withdraw/signup button and your current status (signed up / waitlisted) into the event page.